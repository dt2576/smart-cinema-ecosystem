# Cross-Module Test Artifacts

Use `frontend/`, `backend/`, `integration/`, and `test-cases/` for cross-module test cases, integration test documentation, acceptance artifacts, and manual test specifications.

Keep framework-specific automated tests in their conventional application locations. This structure does not replace or change existing test configuration. No test cases are invented here.
